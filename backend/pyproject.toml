[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Vedant Agrawal", email = "2vedantagrawal@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "uvicorn",
    "fastapi",
    "pandas",
    "pathlib>=1.0.1",
]

[project.scripts]
backend = "backend:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
